'use client';

import TextField from '@mui/material/TextField';

export default function Input() {
    return (
        <TextField
            label="Your Name"
            variant="standard"
        />
    );
}